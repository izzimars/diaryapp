# docs/users/resendOTPCode.yaml
paths:
  /users/resendOTPCode:
    post:
      summary: "Resend OTP Code"
      description: "Resends the OTP code to the user's email"
      tags:
        - Users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  example: elusamisegun6@gmail.com
      responses:
        "200":
          description: "Verification OTP sent"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: PENDING
                  message:
                    type: string
                    example: Verification OTP sent
                  data:
                    type: object
                    properties:
                      email:
                        type: string
                        example: elusamisegun6@gmail.com
        "400":
          description: "Bad Request"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: error
                  message:
                    type: string
                    example: Detailed error message
        "404":
          description: "User has no records"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: error
                  message:
                    type: string
                    example: User has no records
        "500":
          description: "Internal Server Error"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: error
                  message:
                    type: string
                    example: Internal Server Error
